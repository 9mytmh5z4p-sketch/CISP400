
# CISP 400 — Object-Oriented Programming

Canonical lecture slides for CISP 400.

## Migration Status

The lecture folders, source files, assets, and rendered pages were seeded from the P360 project to establish the working structure. Treat their instructional content as provisional until each P400 lecture is replaced.

## Structure
- Each lecture resides in its own folder.
- Rendered HTML is published via GitHub Pages.
- Students are given direct links to lecture HTML files.
- Each completed lecture folder may include a `slide_catalog.csv` file with one row per slide.

## Catalog Files
- `Dictionary_of_Terms.csv` lives at the repository root. It defines the approved category terms used to classify slides.
- `slide_catalog.csv` lives inside an individual lecture folder. It records the slide number, topic, inventory subject, title, category, and asset filename for each slide in that lecture.

## Distribution
This repository is not used for assignments or assessments.

## What's Here?

These are my updated GitHub lectures.
Git requires a single folder with multiple versions. That idea doesn't fit with the way I've been using the materials in the past (copying the prior year's folder).

This folder is Git enabled.
Use aliases for each class, by each semester.
